# This is a direnv .envrc file for Python projects.
# Copy this file to any Python project directory,
# edit as required (see below), and rename .envrc.
#
# There are two options (pick one):

# 1. Conda envs
# NOTE: Replace my_conda_env below as appropriate
eval "$(conda shell.bash hook)"
conda activate my_conda_env

# 2. Virtual envs
source .env/bin/activate
unset PS1
